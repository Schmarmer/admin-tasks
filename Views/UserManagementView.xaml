<Window x:Class="Admin_Tasks.Views.UserManagementView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:Admin_Tasks.ViewModels"
        mc:Ignorable="d"
        Title="{Binding Title}"
        Height="700" Width="1200"
        MinHeight="600" MinWidth="900"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}"
        d:DataContext="{d:DesignInstance Type=viewmodels:UserManagementViewModel}">
    
    <Window.Resources>
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        
        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
        
        <Style x:Key="FormLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
            <Setter Property="Margin" Value="0,0,0,3"/>
            <Setter Property="VerticalAlignment" Value="Bottom"/>
        </Style>
        
        <Style x:Key="FormTextBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
        
        <Style x:Key="FormPasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
        
        <Style x:Key="FormComboBoxStyle" TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
        
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource AccentHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource AccentPressedBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="{DynamicResource DisabledBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource DisabledForegroundBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource SecondaryBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
        </Style>
        
        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource DangerBrush}"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Benutzerverwaltung" Style="{StaticResource HeaderTextStyle}"/>
        
        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Users List -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- Search -->
                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBox Grid.Column="0"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource FormTextBoxStyle}"
                             Margin="0,0,10,0"
                             FontSize="14"
                             Padding="10,8">
                        <TextBox.Resources>
                            <Style TargetType="TextBox" BasedOn="{StaticResource FormTextBoxStyle}">
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="">
                                        <Setter Property="Background">
                                            <Setter.Value>
                                                <VisualBrush AlignmentX="Left" AlignmentY="Center" Stretch="None">
                                                    <VisualBrush.Visual>
                                                        <TextBlock Text="Benutzer suchen..." 
                                                                   Foreground="{DynamicResource PlaceholderBrush}" 
                                                                   FontSize="14" 
                                                                   Margin="10,0,0,0"/>
                                                    </VisualBrush.Visual>
                                                </VisualBrush>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Resources>
                    </TextBox>
                    
                    <Button Grid.Column="1"
                            Content="🔍"
                            Command="{Binding SearchCommand}"
                            Style="{StaticResource ActionButtonStyle}"
                            Padding="10,8"/>
                </Grid>
                
                <!-- Users DataGrid -->
                <DataGrid Grid.Row="2"
                          ItemsSource="{Binding UsersView}"
                          SelectedItem="{Binding SelectedUser}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="True"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          Background="{DynamicResource BackgroundBrush}"
                          Foreground="{DynamicResource ForegroundBrush}"
                          BorderBrush="{DynamicResource BorderBrush}"
                          BorderThickness="1">
                    
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Benutzername" Binding="{Binding Username}" Width="150"/>
                        <DataGridTextColumn Header="Name" Binding="{Binding FullName}" Width="200"/>
                        <DataGridTextColumn Header="E-Mail" Binding="{Binding Email}" Width="200"/>
                        <DataGridTextColumn Header="Rolle" Binding="{Binding Role}" Width="100"/>
                        <DataGridCheckBoxColumn Header="Aktiv" Binding="{Binding IsActive}" Width="60"/>
                        <DataGridTextColumn Header="Erstellt" Binding="{Binding CreatedAt, StringFormat=dd.MM.yyyy}" Width="100"/>
                        <DataGridTextColumn Header="Letzter Login" Binding="{Binding LastLoginAt, StringFormat=dd.MM.yyyy HH:mm}" Width="130"/>
                        
                        <DataGridTemplateColumn Header="Aktionen" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Content="✏️"
                                                Command="{Binding DataContext.EditUserCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}"
                                                Style="{StaticResource SecondaryButtonStyle}"
                                                Padding="6,4"
                                                Margin="2,0"
                                                ToolTip="Bearbeiten"/>
                                        <Button Content="🗑️"
                                                Command="{Binding DataContext.DeleteUserCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}"
                                                Style="{StaticResource DangerButtonStyle}"
                                                Padding="6,4"
                                                Margin="2,0"
                                                ToolTip="Löschen"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                    
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="Transparent"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource HoverBrush}"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>
                </DataGrid>
            </Grid>
            
            <!-- Separator -->
            <Rectangle Grid.Column="1" Fill="{DynamicResource BorderBrush}" Width="1" Margin="5,0"/>
            
            <!-- User Form -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Form Header -->
                <TextBlock Grid.Row="0" 
                           Style="{StaticResource SectionHeaderStyle}"
                           Margin="0,0,0,15">
                    <TextBlock.Text>
                        <MultiBinding StringFormat="{}{0}">
                            <Binding Path="IsEditing" Converter="{StaticResource BooleanToStringConverter}" ConverterParameter="Benutzer bearbeiten|Neuen Benutzer erstellen"/>
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
                
                <!-- Form Fields -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!-- Username -->
                        <TextBlock Text="Benutzername *" Style="{StaticResource FormLabelStyle}"/>
                        <TextBox Text="{Binding NewUser.Username, UpdateSourceTrigger=PropertyChanged}" 
                                 Style="{StaticResource FormTextBoxStyle}"/>
                        
                        <!-- Email -->
                        <TextBlock Text="E-Mail *" Style="{StaticResource FormLabelStyle}"/>
                        <TextBox Text="{Binding NewUser.Email, UpdateSourceTrigger=PropertyChanged}" 
                                 Style="{StaticResource FormTextBoxStyle}"/>
                        
                        <!-- First Name -->
                        <TextBlock Text="Vorname *" Style="{StaticResource FormLabelStyle}"/>
                        <TextBox Text="{Binding NewUser.FirstName, UpdateSourceTrigger=PropertyChanged}" 
                                 Style="{StaticResource FormTextBoxStyle}"/>
                        
                        <!-- Last Name -->
                        <TextBlock Text="Nachname *" Style="{StaticResource FormLabelStyle}"/>
                        <TextBox Text="{Binding NewUser.LastName, UpdateSourceTrigger=PropertyChanged}" 
                                 Style="{StaticResource FormTextBoxStyle}"/>
                        
                        <!-- Role -->
                        <TextBlock Text="Rolle *" Style="{StaticResource FormLabelStyle}"/>
                        <ComboBox ItemsSource="{Binding AvailableRoles}"
                                  SelectedItem="{Binding NewUser.Role}"
                                  Style="{StaticResource FormComboBoxStyle}"/>
                        
                        <!-- Password -->
                        <TextBlock Style="{StaticResource FormLabelStyle}">
                            <TextBlock.Text>
                                <MultiBinding StringFormat="{}{0}">
                                    <Binding Path="IsEditing" Converter="{StaticResource BooleanToStringConverter}" ConverterParameter="Neues Passwort|Passwort *"/>
                                </MultiBinding>
                            </TextBlock.Text>
                        </TextBlock>
                        <PasswordBox x:Name="PasswordBox" Style="{StaticResource FormPasswordBoxStyle}"/>
                        
                        <!-- Confirm Password -->
                        <TextBlock Style="{StaticResource FormLabelStyle}">
                            <TextBlock.Text>
                                <MultiBinding StringFormat="{}{0}">
                                    <Binding Path="IsEditing" Converter="{StaticResource BooleanToStringConverter}" ConverterParameter="Passwort bestätigen|Passwort bestätigen *"/>
                                </MultiBinding>
                            </TextBlock.Text>
                        </TextBlock>
                        <PasswordBox x:Name="ConfirmPasswordBox" Style="{StaticResource FormPasswordBoxStyle}"/>
                        
                        <!-- Is Active -->
                        <CheckBox Content="Benutzer ist aktiv" 
                                  IsChecked="{Binding NewUser.IsActive}"
                                  Foreground="{DynamicResource ForegroundBrush}"
                                  Margin="0,10,0,20"/>
                        
                        <!-- Required Fields Note -->
                        <TextBlock Text="* Pflichtfelder" 
                                   FontSize="10" 
                                   FontStyle="Italic" 
                                   Foreground="{DynamicResource PlaceholderBrush}"
                                   Margin="0,0,0,10"/>
                    </StackPanel>
                </ScrollViewer>
                
                <!-- Form Actions -->
                <StackPanel Grid.Row="2" Orientation="Vertical" Margin="0,15,0,0">
                    <!-- Primary Actions -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" Margin="0,0,0,10">
                        <Button Content="Speichern"
                                Command="{Binding SaveUserCommand}"
                                Style="{StaticResource ActionButtonStyle}"
                                HorizontalAlignment="Stretch"
                                Margin="0,0,5,0"
                                Visibility="{Binding IsEditing, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        
                        <Button Content="Erstellen"
                                Command="{Binding AddUserCommand}"
                                Style="{StaticResource ActionButtonStyle}"
                                HorizontalAlignment="Stretch"
                                Margin="0,0,5,0"
                                Visibility="{Binding IsEditing, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>
                        
                        <Button Content="Abbrechen"
                                Command="{Binding CancelEditCommand}"
                                Style="{StaticResource SecondaryButtonStyle}"
                                HorizontalAlignment="Stretch"
                                Margin="5,0,0,0"/>
                    </StackPanel>
                    
                    <!-- Secondary Actions -->
                    <Button Content="Aktualisieren"
                            Command="{Binding LoadUsersCommand}"
                            Style="{StaticResource SecondaryButtonStyle}"
                            HorizontalAlignment="Stretch"/>
                </StackPanel>
            </Grid>
        </Grid>
        
        <!-- Loading Overlay -->
        <Grid Grid.Row="0" Grid.RowSpan="2"
              Background="#80000000"
              Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Border Background="{DynamicResource BackgroundBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="30"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <ProgressBar IsIndeterminate="True" Width="20" Height="20" Margin="0,0,10,0"/>
                    <TextBlock Text="Wird geladen..." 
                               Foreground="{DynamicResource ForegroundBrush}"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>