<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
    ================================================================
    DARK THEME
    ================================================================
    - Author: Your Name
    - Date: 14.08.2025
    - Description: A sophisticated and modern dark theme.
    ================================================================
    -->

    <!-- Base Palette -->
    <Color x:Key="ColorBlack">#000000</Color>
    <Color x:Key="ColorWhite">#FFFFFF</Color>
    <Color x:Key="ColorGray800">#2B2B2B</Color>
    <Color x:Key="ColorGray850">#3C3C3C</Color>
    <Color x:Key="ColorGray900">#1E1E1E</Color>
    <Color x:Key="ColorGray700">#5A5A5A</Color>
    <Color x:Key="ColorGray100">#F0F0F0</Color>
    <Color x:Key="ColorGray200">#D0D0D0</Color>
    <Color x:Key="ColorBlue400">#4A9EFF</Color>
    <Color x:Key="ColorBlue500">#6BB6FF</Color>
    <Color x:Key="ColorGreen400">#4CAF50</Color>
    <Color x:Key="ColorYellow400">#FF9800</Color>
    <Color x:Key="ColorRed400">#F44336</Color>
    <Color x:Key="ColorCyan400">#2196F3</Color>
    <Color x:Key="ShadowColor">#40000000</Color>

    <!-- Semantic Brushes -->
    <!-- Backgrounds -->
    <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource ColorGray900}"/>
    <SolidColorBrush x:Key="SecondaryBackgroundBrush" Color="{StaticResource ColorGray800}"/>
    <SolidColorBrush x:Key="HeaderBackgroundBrush" Color="{StaticResource ColorGray850}"/>
    <SolidColorBrush x:Key="CardBackgroundBrush" Color="{StaticResource ColorGray800}"/>
    <SolidColorBrush x:Key="HoverBackgroundBrush" Color="{StaticResource ColorGray700}" Opacity="0.5"/>

    <!-- Text -->
    <SolidColorBrush x:Key="TextBrush" Color="{StaticResource ColorGray100}"/>
    <SolidColorBrush x:Key="SecondaryTextBrush" Color="{StaticResource ColorGray200}" Opacity="0.8"/>
    <SolidColorBrush x:Key="AccentTextBrush" Color="{StaticResource ColorWhite}"/>

    <!-- Accents -->
    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource ColorBlue400}"/>
    <SolidColorBrush x:Key="AccentHoverBrush" Color="{StaticResource ColorBlue500}"/>
    <SolidColorBrush x:Key="SelectionBackgroundBrush" Color="{StaticResource ColorBlue400}" Opacity="0.25"/>

    <!-- Borders -->
    <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource ColorGray700}"/>
    <SolidColorBrush x:Key="FocusBorderBrush" Color="{StaticResource ColorBlue400}"/>

    <!-- Status -->
    <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource ColorGreen400}"/>
    <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource ColorYellow400}"/>
    <SolidColorBrush x:Key="DangerBrush" Color="{StaticResource ColorRed400}"/>
    <SolidColorBrush x:Key="InfoBrush" Color="{StaticResource ColorCyan400}"/>

    <!-- Other -->
    <SolidColorBrush x:Key="ShadowBrush" Color="{StaticResource ShadowColor}"/>
    
    <!-- News-specific Brushes -->
    <SolidColorBrush x:Key="AccentBackgroundBrush" Color="{StaticResource ColorBlue400}" Opacity="0.15"/>
    <SolidColorBrush x:Key="UnreadIndicatorBrush" Color="{StaticResource ColorBlue400}"/>
    <SolidColorBrush x:Key="NewsCardBrush" Color="{StaticResource ColorGray800}"/>
    <SolidColorBrush x:Key="NewsHoverBrush" Color="{StaticResource ColorGray700}" Opacity="0.7"/>
    <SolidColorBrush x:Key="NewsTextBrush" Color="{StaticResource ColorGray100}"/>
    <SolidColorBrush x:Key="NewsSecondaryTextBrush" Color="{StaticResource ColorGray200}" Opacity="0.8"/>
    <SolidColorBrush x:Key="NewsTimestampBrush" Color="{StaticResource ColorGray200}" Opacity="0.6"/>

    <!-- Legacy Aliases for Compatibility -->
    <SolidColorBrush x:Key="PrimaryBackgroundBrush" Color="{StaticResource ColorGray900}"/>
    <SolidColorBrush x:Key="TertiaryBackgroundBrush" Color="{StaticResource ColorGray700}"/>
    <SolidColorBrush x:Key="PrimaryTextBrush" Color="{StaticResource ColorGray100}"/>
    
    <!-- UserManagement Specific Brushes -->
    <SolidColorBrush x:Key="ForegroundBrush" Color="{StaticResource ColorGray100}"/>
    <SolidColorBrush x:Key="SecondaryForegroundBrush" Color="{StaticResource ColorGray200}" Opacity="0.9"/>
    <SolidColorBrush x:Key="TertiaryForegroundBrush" Color="{StaticResource ColorGray200}" Opacity="0.7"/>
    <SolidColorBrush x:Key="InputBackgroundBrush" Color="{StaticResource ColorGray800}"/>
    <SolidColorBrush x:Key="PlaceholderBrush" Color="#99D0D0D0"/>
    <SolidColorBrush x:Key="HoverBrush" Color="#4D5A5A5A"/>
    <SolidColorBrush x:Key="SecondaryBrush" Color="{StaticResource ColorGray700}"/>
    <SolidColorBrush x:Key="AccentPressedBrush" Color="#CC4A9EFF"/>
    <SolidColorBrush x:Key="DisabledBrush" Color="#805A5A5A"/>
    <SolidColorBrush x:Key="DisabledForegroundBrush" Color="#66D0D0D0"/>

    <!-- ComboBox Style for Dark Theme -->
    <Style TargetType="ComboBox">
        <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton Name="ToggleButton" 
                                      Background="{TemplateBinding Background}"
                                      BorderBrush="{TemplateBinding BorderBrush}"
                                      BorderThickness="{TemplateBinding BorderThickness}"
                                      Foreground="{TemplateBinding Foreground}"
                                      IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="4">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="20"/>
                                            </Grid.ColumnDefinitions>
                                            <Path Grid.Column="1" 
                                                  HorizontalAlignment="Center" 
                                                  VerticalAlignment="Center"
                                                  Data="M 0 0 L 4 4 L 8 0 Z"
                                                  Fill="{TemplateBinding Foreground}"/>
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{StaticResource HoverBackgroundBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <ContentPresenter Name="ContentSite"
                                          IsHitTestVisible="False"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Left"/>
                        <Popup Name="Popup"
                               Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True"
                               Focusable="False"
                               PopupAnimation="Slide">
                            <Grid Name="DropDown"
                                  SnapsToDevicePixels="True"
                                  MinWidth="{TemplateBinding ActualWidth}"
                                  MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                <Border Name="DropDownBorder"
                                        Background="{StaticResource CardBackgroundBrush}"
                                        BorderBrush="{StaticResource BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="4"
                                        Margin="0,1,0,0">
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Grid>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ComboBoxItem Style for Dark Theme -->
    <Style TargetType="ComboBoxItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource HoverBackgroundBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ContextMenu Style for Dark Theme -->
    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        <Setter Property="Padding" Value="4"/>
        <Setter Property="HasDropShadow" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}">
                        <Border.Effect>
                            <DropShadowEffect Color="Black" 
                                              Direction="315" 
                                              ShadowDepth="4" 
                                              BlurRadius="8" 
                                              Opacity="0.3"/>
                        </Border.Effect>
                        <ScrollViewer Style="{DynamicResource {ComponentResourceKey ResourceId=MenuScrollViewer, TypeInTargetAssembly={x:Type FrameworkElement}}}">
                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle" 
                                            Grid.IsSharedSizeScope="True"/>
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- MenuItem Style for Dark Theme -->
    <Style TargetType="MenuItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="MinHeight" Value="32"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border Name="Border" 
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4"
                            Margin="2">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Icon"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Shortcut"/>
                                <ColumnDefinition Width="13"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Icon -->
                            <ContentPresenter Name="Icon"
                                              Grid.Column="0"
                                              Margin="6,0,6,0"
                                              VerticalAlignment="Center"
                                              ContentSource="Icon"/>
                            
                            <!-- Header Text -->
                            <ContentPresenter Name="HeaderHost"
                                              Grid.Column="1"
                                              ContentSource="Header"
                                              RecognizesAccessKey="True"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                            
                            <!-- Shortcut Text -->
                            <TextBlock Name="InputGestureText"
                                       Grid.Column="2"
                                       Text="{TemplateBinding InputGestureText}"
                                       Margin="5,2,0,2"
                                       DockPanel.Dock="Right"
                                       VerticalAlignment="Center"/>
                            
                            <!-- Submenu Arrow -->
                            <Path Name="ArrowPath"
                                  Grid.Column="3"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  Data="M 0 0 L 0 7 L 4 3.5 Z"
                                  Fill="{TemplateBinding Foreground}"
                                  Visibility="Collapsed"/>
                            
                            <!-- Submenu Popup -->
                            <Popup Name="Popup"
                                   Placement="Right"
                                   IsOpen="{TemplateBinding IsSubmenuOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Fade">
                                <Border Name="SubmenuBorder"
                                        Background="{StaticResource CardBackgroundBrush}"
                                        BorderBrush="{StaticResource BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Padding="4">
                                    <Border.Effect>
                                        <DropShadowEffect Color="Black" 
                                                          Direction="315" 
                                                          ShadowDepth="4" 
                                                          BlurRadius="8" 
                                                          Opacity="0.3"/>
                                    </Border.Effect>
                                    <ScrollViewer Style="{DynamicResource {ComponentResourceKey ResourceId=MenuScrollViewer, TypeInTargetAssembly={x:Type FrameworkElement}}}">
                                        <StackPanel IsItemsHost="True" 
                                                    KeyboardNavigation.DirectionalNavigation="Cycle"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </Border>
                    
                    <ControlTemplate.Triggers>
                        <!-- Submenu Indicator -->
                        <Trigger Property="Role" Value="SubmenuHeader">
                            <Setter TargetName="ArrowPath" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        
                        <!-- Hover Effect -->
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource AccentBrush}"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        
                        <!-- Disabled State -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}"/>
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                        
                        <!-- Pressed State -->
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource AccentHoverBrush}"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- DataGrid Style for Dark Theme -->
    <Style TargetType="DataGrid">
        <Setter Property="Background" Value="{StaticResource BackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="GridLinesVisibility" Value="Horizontal"/>
        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="VerticalGridLinesBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="RowBackground" Value="Transparent"/>
        <Setter Property="AlternatingRowBackground" Value="#4D3C3C3C"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
    </Style>

    <!-- DataGridColumnHeader Style for Dark Theme -->
    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{StaticResource HeaderBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,1,1"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>

    <!-- DataGridCell Style for Dark Theme -->
    <Style TargetType="DataGridCell">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridCell">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                 <Setter Property="Background" Value="#4D4A9EFF"/>
                 <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
             </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>