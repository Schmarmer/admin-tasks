<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
    ================================================================
    LIGHT THEME
    ================================================================
    - Author: Your Name
    - Date: 14.08.2025
    - Description: A clean and modern light theme for the application.
    ================================================================
    -->

    <!-- Base Palette -->
    <Color x:Key="ColorWhite">#FFFFFF</Color>
    <Color x:Key="ColorGray100">#F8F9FA</Color>
    <Color x:Key="ColorGray200">#E9ECEF</Color>
    <Color x:Key="ColorGray300">#DEE2E6</Color>
    <Color x:Key="ColorGray700">#495057</Color>
    <Color x:Key="ColorBlack">#000000</Color>
    <Color x:Key="ColorBlue500">#0D6EFD</Color>
    <Color x:Key="ColorBlue600">#0B5ED7</Color>
    <Color x:Key="ColorGreen500">#198754</Color>
    <Color x:Key="ColorYellow500">#FFC107</Color>
    <Color x:Key="ColorRed500">#DC3545</Color>
    <Color x:Key="ColorCyan500">#0DCAF0</Color>
    <Color x:Key="ShadowColor">#1A000000</Color>

    <!-- Semantic Brushes -->
    <!-- Backgrounds -->
    <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource ColorWhite}"/>
    <SolidColorBrush x:Key="SecondaryBackgroundBrush" Color="{StaticResource ColorGray100}"/>
    <SolidColorBrush x:Key="HeaderBackgroundBrush" Color="{StaticResource ColorWhite}"/>
    <SolidColorBrush x:Key="CardBackgroundBrush" Color="{StaticResource ColorWhite}"/>
    <SolidColorBrush x:Key="HoverBackgroundBrush" Color="{StaticResource ColorGray200}"/>

    <!-- Text -->
    <SolidColorBrush x:Key="TextBrush" Color="{StaticResource ColorGray700}"/>
    <SolidColorBrush x:Key="SecondaryTextBrush" Color="{StaticResource ColorGray700}" Opacity="0.7"/>
    <SolidColorBrush x:Key="AccentTextBrush" Color="{StaticResource ColorWhite}"/>

    <!-- Accents -->
    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource ColorBlue500}"/>
    <SolidColorBrush x:Key="AccentHoverBrush" Color="{StaticResource ColorBlue600}"/>
    <SolidColorBrush x:Key="SelectionBackgroundBrush" Color="{StaticResource ColorBlue500}" Opacity="0.12"/>

    <!-- Borders -->
    <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource ColorGray300}"/>
    <SolidColorBrush x:Key="FocusBorderBrush" Color="{StaticResource ColorBlue500}"/>

    <!-- Inputs -->
    <SolidColorBrush x:Key="InputBackgroundBrush" Color="{StaticResource ColorWhite}"/>
    <SolidColorBrush x:Key="SecondaryButtonBrush" Color="{StaticResource ColorGray200}"/>
    
    <!-- Status -->
    <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource ColorGreen500}"/>
    <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource ColorYellow500}"/>
    <SolidColorBrush x:Key="DangerBrush" Color="{StaticResource ColorRed500}"/>
    <SolidColorBrush x:Key="InfoBrush" Color="{StaticResource ColorCyan500}"/>

    <!-- Other -->
    <SolidColorBrush x:Key="ShadowBrush" Color="{StaticResource ShadowColor}"/>

    <!-- Legacy Aliases for Compatibility -->
    <SolidColorBrush x:Key="PrimaryBackgroundBrush" Color="{StaticResource ColorWhite}"/>
    <SolidColorBrush x:Key="TertiaryBackgroundBrush" Color="{StaticResource ColorGray200}"/>
    <SolidColorBrush x:Key="PrimaryTextBrush" Color="{StaticResource ColorGray700}"/>
    
    <!-- Additional Foreground Brushes -->
    <SolidColorBrush x:Key="ForegroundBrush" Color="{StaticResource ColorGray700}"/>
    <SolidColorBrush x:Key="SecondaryForegroundBrush" Color="{StaticResource ColorGray700}" Opacity="0.8"/>
    <SolidColorBrush x:Key="TertiaryForegroundBrush" Color="{StaticResource ColorGray700}" Opacity="0.6"/>

    <!-- ComboBox Style for Light Theme -->
    <Style TargetType="ComboBox">
        <Setter Property="Background" Value="{StaticResource InputBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton Name="ToggleButton" 
                                      Background="{TemplateBinding Background}"
                                      BorderBrush="{TemplateBinding BorderBrush}"
                                      BorderThickness="{TemplateBinding BorderThickness}"
                                      Foreground="{TemplateBinding Foreground}"
                                      IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="4">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="20"/>
                                            </Grid.ColumnDefinitions>
                                            <Path Grid.Column="1" 
                                                  HorizontalAlignment="Center" 
                                                  VerticalAlignment="Center"
                                                  Data="M 0 0 L 4 4 L 8 0 Z"
                                                  Fill="{TemplateBinding Foreground}"/>
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{StaticResource HoverBackgroundBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <ContentPresenter Name="ContentSite"
                                          IsHitTestVisible="False"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Left"/>
                        <Popup Name="Popup"
                               Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True"
                               Focusable="False"
                               PopupAnimation="Slide">
                            <Grid Name="DropDown"
                                  SnapsToDevicePixels="True"
                                  MinWidth="{TemplateBinding ActualWidth}"
                                  MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                <Border Name="DropDownBorder"
                                        Background="{StaticResource InputBackgroundBrush}"
                                        BorderBrush="{StaticResource BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="4"
                                        Margin="0,1,0,0">
                                    <Border.Effect>
                                        <DropShadowEffect Color="{StaticResource ShadowColor}" ShadowDepth="2" BlurRadius="8" Opacity="0.3"/>
                                    </Border.Effect>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Grid>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ComboBoxItem Style for Light Theme -->
    <Style TargetType="ComboBoxItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource HoverBackgroundBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ContextMenu Style for Light Theme -->
    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        <Setter Property="Padding" Value="4"/>
        <Setter Property="HasDropShadow" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}">
                        <Border.Effect>
                            <DropShadowEffect Color="{StaticResource ShadowColor}" 
                                              Direction="315" 
                                              ShadowDepth="4" 
                                              BlurRadius="8" 
                                              Opacity="0.15"/>
                        </Border.Effect>
                        <ScrollViewer Style="{DynamicResource {ComponentResourceKey ResourceId=MenuScrollViewer, TypeInTargetAssembly={x:Type FrameworkElement}}}">
                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle" 
                                            Grid.IsSharedSizeScope="True"/>
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- MenuItem Style for Light Theme -->
    <Style TargetType="MenuItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="MinHeight" Value="32"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border Name="Border" 
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4"
                            Margin="2">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Icon"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Shortcut"/>
                                <ColumnDefinition Width="13"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Icon -->
                            <ContentPresenter Name="Icon"
                                              Grid.Column="0"
                                              Margin="6,0,6,0"
                                              VerticalAlignment="Center"
                                              ContentSource="Icon"/>
                            
                            <!-- Header Text -->
                            <ContentPresenter Name="HeaderHost"
                                              Grid.Column="1"
                                              ContentSource="Header"
                                              RecognizesAccessKey="True"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                            
                            <!-- Shortcut Text -->
                            <TextBlock Name="InputGestureText"
                                       Grid.Column="2"
                                       Text="{TemplateBinding InputGestureText}"
                                       Margin="5,2,0,2"
                                       DockPanel.Dock="Right"
                                       VerticalAlignment="Center"/>
                            
                            <!-- Submenu Arrow -->
                            <Path Name="ArrowPath"
                                  Grid.Column="3"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  Data="M 0 0 L 0 7 L 4 3.5 Z"
                                  Fill="{TemplateBinding Foreground}"
                                  Visibility="Collapsed"/>
                            
                            <!-- Submenu Popup -->
                            <Popup Name="Popup"
                                   Placement="Right"
                                   IsOpen="{TemplateBinding IsSubmenuOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Fade">
                                <Border Name="SubmenuBorder"
                                        Background="{StaticResource CardBackgroundBrush}"
                                        BorderBrush="{StaticResource BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Padding="4">
                                    <Border.Effect>
                                        <DropShadowEffect Color="{StaticResource ShadowColor}" 
                                                          Direction="315" 
                                                          ShadowDepth="4" 
                                                          BlurRadius="8" 
                                                          Opacity="0.15"/>
                                    </Border.Effect>
                                    <ScrollViewer Style="{DynamicResource {ComponentResourceKey ResourceId=MenuScrollViewer, TypeInTargetAssembly={x:Type FrameworkElement}}}">
                                        <StackPanel IsItemsHost="True" 
                                                    KeyboardNavigation.DirectionalNavigation="Cycle"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </Border>
                    
                    <ControlTemplate.Triggers>
                        <!-- Submenu Indicator -->
                        <Trigger Property="Role" Value="SubmenuHeader">
                            <Setter TargetName="ArrowPath" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        
                        <!-- Hover Effect -->
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource AccentBrush}"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        
                        <!-- Disabled State -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}"/>
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                        
                        <!-- Pressed State -->
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource AccentHoverBrush}"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>